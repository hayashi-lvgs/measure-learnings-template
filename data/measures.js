/* =========================================================================
 * ★施策データの「正」— この事業部リポジトリで共有される施策はすべてここ
 * -------------------------------------------------------------------------
 * 1施策の追加は、原則 MEASURES 配列にオブジェクトを1つ足すだけで完結する。
 * PDFから下書きを作る場合は tools/import.html でスニペットを生成し、ここに貼る。
 *
 * ⚠️ このファイルにコミットした施策だけが、リポジトリを見る全員に共有される。
 *    ブラウザ上のドロップ取り込みは「そのブラウザだけ」の一時プレビュー。
 * ⚠️ fetch()／type="module" は使わない（file:// で動かすため classic script）。
 *
 * データ構造の実例は docs/施策データの書き方.md を参照。
 * ========================================================================= */

// ===== 施策一覧（win=勝ち / draw=引き分け / loss=負け） =====
const MEASURES = [
  // ここに施策オブジェクトを追加する。空でもサイトは正常に動く。
];

// ===== 横断インサイト（「横断インサイト」タブに表示） =====
// 施策が貯まってきたら、横断で見えたパターンと外れた仮説をここに書く。
const INSIGHTS = {
  patterns: [
    // { icon: "💡", text: "…", sources: ["ID-001"], type: "pos" }  // type: pos|neg|neu
  ],
  hypothesisMisses: [
    // { text: "…（立てた仮説）", source: "ID-001", lesson: "…（学び）" }
  ]
};

// ===== スクショ対応表（施策ID → 前後比較画像のパス） =====
// images/ に画像を置き、ここに1行足すとカードにサムネが出る。無い施策は書かない。
const SCREENSHOTS = {
  // "ID-001": "images/ID-001-before-after.png"
};
